class UserObj
{
  final String? uid;
  UserObj(this.uid,);
}
class UserData{
  final String? fn;
  final String? ln;
  final String? acc_type;
  UserData(this.fn, this.ln, this.acc_type);
}
class RiderModel{
  final String? id;
  final String fn;
  final String ln;
  RiderModel(this.id, this.fn, this.ln);
}